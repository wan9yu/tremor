# Internal documentation (not for public repo)
internal/

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/

# macOS
.DS_Store

# Editor / IDE
.idea/
.vscode/
*.swp

# Logs and temp
*.log
tmp/

# BGP seed fetch cache (bulky per-country diagnostic scratch; the seeded line
# CSV is the committed record, this cache only makes the sweep resumable)
data/archive/bgp_seed_cache/
